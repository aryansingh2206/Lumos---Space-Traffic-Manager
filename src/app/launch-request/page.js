'use client';

import LaunchRequestForm from '../../components/LaunchRequestForm';

export default function LaunchRequestPage() {
  return (
    <main style={{ textAlign: 'center', marginTop: '60px' }}>
      <h2> Submit Launch Request</h2>
      <LaunchRequestForm />
    </main>
  );
}
